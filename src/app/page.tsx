import { BuilderShell } from "@/components/builder/BuilderShell";

// This is an RSC page — the heavy lifting is pushed to client components below
export default function Home() {
  return <BuilderShell />;
}
